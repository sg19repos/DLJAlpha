import React from 'react';
import * as headerStyles from '../../styles/headerStyles.css';

const Header = () => {
    return (
        <>
            <div className={headerStyles.headerTitle}>DL Jewels</div>
        </>
    );
};

export default Header;
